[project]
name = "bmcu-debug-tools"
version = "0.1.0"
description = "Safe host-side monitor and command client for BMCU Link"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "pyserial>=3.5,<4",
]

[tool.uv]
package = false
default-groups = []
